public class Formatter {
    private StringBuilder output;
    private char[] buff;

    public Formatter() {
        this.output = new StringBuilder();
        this.buff = new char[2];
    }

    public String format(String input) {
        char[] in = input.toCharArray();
        for ()
    }
}
